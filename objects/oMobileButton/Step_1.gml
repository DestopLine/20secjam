
   
    vstick_step(1)


/*	// Assuming obj_object is the object you want to keep on screen
var screenWidth = display_get_width();
var screenHeight = display_get_height();

var offsetX = 32; // Adjust the offset as needed
var offsetY = 32; // Adjust the offset as needed

x = clamp(x, offsetX, screenWidth - offsetX);
y = clamp(y, offsetY, screenHeight - offsetY);


	switch buttondir
	{
	case "left":
	image_index = 1;
	break;
	case "right":
	image_index = 0;
	break;
	case "up":
	image_index = 2;
	break;
	case "down":
	image_index = 3;
	break;
	}


































